# Jodex AI Assistant - Environment Configuration Template
# Copy this file to .env.local and fill in your actual API keys

# ===================================================================
# OPENAI CONFIGURATION
# ===================================================================
# Get your API key from: https://platform.openai.com/api-keys
# You'll need an OpenAI account with billing enabled

# Client-side OpenAI API key (public - used by the component)
NEXT_PUBLIC_OPENAI_API_KEY=sk-your-openai-api-key-here

# Server-side OpenAI API key (private - used by API routes)
# You can use the same key as above, but server-side usage is recommended
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===================================================================
# LIVEKIT CONFIGURATION (Optional - for voice features)
# ===================================================================
# LiveKit is required for real-time voice communication
# You can use a free LiveKit Cloud account or self-host a server

# Your LiveKit server URL (public)
# Examples: wss://your-livekit-server.com or wss://your-project.livekit.cloud
NEXT_PUBLIC_LIVEKIT_URL=wss://your-livekit-server.com

# LiveKit API credentials (private - used for token generation)
# Get these from your LiveKit Cloud dashboard or server configuration
LIVEKIT_API_KEY=your-livekit-api-key-here
LIVEKIT_API_SECRET=your-livekit-secret-key-here

# Optional: Pre-generated LiveKit token (if you have one)
# This is optional - the app can generate tokens server-side
# NEXT_PUBLIC_LIVEKIT_TOKEN=your-livekit-token-here

# ===================================================================
# APPLICATION SETTINGS
# ===================================================================

# Development mode (shows debug information)
# Set to "true" for development, "false" for production
NEXT_PUBLIC_DEBUG=false

# ===================================================================
# USAGE INSTRUCTIONS
# ===================================================================
#
# 1. Copy this file to .env.local:
#    cp .env.example .env.local
#
# 2. Replace placeholder values with your actual API keys:
#    - Get OpenAI API key from platform.openai.com
#    - Get LiveKit credentials from your LiveKit dashboard
#
# 3. Install dependencies:
#    npm install
#
# 4. Run the development server:
#    npm run dev
#
# 5. Open http://localhost:3000 to test the application
#
# NOTE: Never commit your .env.local file to version control!
#       It's already included in .gitignore for your safety.
#
# ===================================================================
# TESTING WITHOUT VOICE FEATURES
# ===================================================================
# If you want to test just the chat functionality without voice:
# - Only set the OPENAI_API_KEY values
# - Leave LiveKit values as placeholder
# - Set voiceEnabled={false} in the JodexAI component
#
# ===================================================================
# TROUBLESHOOTING
# ===================================================================
#
# OpenAI Issues:
# - Make sure your OpenAI account has billing enabled
# - Check that your API key has sufficient credits
# - Verify the key starts with "sk-"
#
# LiveKit Issues:
# - Ensure the URL uses "wss://" (WebSocket Secure)
# - Verify your API key and secret are correct
# - Check that your LiveKit server is running
#
# General Issues:
# - Make sure .env.local is in the same directory as package.json
# - Restart the development server after changing environment variables
# - Check the browser console for detailed error messages
#
